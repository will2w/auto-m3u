name: IPTV Auto Builder Pro

on:
  schedule:
    # 修正：每 4 小时自动更新 (标准 Cron 语法: 分 时 日 月 周)
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - name: 📦 Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.PAT_TOKEN }}

      # 2. 安装必需工具
      - name: 🛠️ Install Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      # 3. 下载 M3U 源列表
      - name: ⬇️ Download Source Lists
        run: |
          echo "Downloading M3U sources..."
          curl -L --retry 3 -o china.m3u    https://freetv.fun/test_channels_china_new.m3u
          curl -L --retry 3 -o hongkong.m3u https://freetv.fun/test_channels_hong_kong_new.m3u
          curl -L --retry 3 -o taiwan.m3u   https://freetv.fun/test_channels_taiwan_new.m3u

      # 4. 下载 EPG
      - name: ⬇️ Download EPG
        run: |
          echo "Downloading EPG file..."
          curl -L --retry 3 -o epg.xml http://epg.51zmt.top:8000/e.xml

      # 5. 生成 playlist
      - name: ⚙️ Build and Filter Playlist
        run: |
          mkdir -p output
          
          # 【修改点】使用 echo 替代 cat EOF，避免 YAML 缩进错误导致 FLOW 不显示
          echo "#EXTM3U" > output/playlist.m3u

          # 声明关联数组
          declare -A seen_urls
          declare -A seen_names

          # 中文省份映射表
          declare -A PROVINCE_MAP=( 
            ["Beijing"]="北京" ["Shanghai"]="上海" ["Tianjin"]="天津" ["Chongqing"]="重庆" 
            ["Hebei"]="河北" ["Shanxi"]="山西" ["InnerMongolia"]="内蒙古" ["Liaoning"]="辽宁" 
            ["Jilin"]="吉林" ["Heilongjiang"]="黑龙江" ["Jiangsu"]="江苏" ["Zhejiang"]="浙江" 
            ["Anhui"]="安徽" ["Fujian"]="福建" ["Jiangxi"]="江西" ["Shandong"]="山东" 
            ["Henan"]="河南" ["Hubei"]="湖北" ["Hunan"]="湖南" ["Guangdong"]="广东" 
            ["Guangxi"]="广西" ["Hainan"]="海南" ["Sichuan"]="四川" ["Guizhou"]="贵州" 
            ["Yunnan"]="云南" ["Tibet"]="西藏" ["Shaanxi"]="陕西" ["Gansu"]="甘肃" 
            ["Qinghai"]="青海" ["Ningxia"]="宁夏" ["Xinjiang"]="新疆" ["HongKong"]="香港" 
            ["Macau"]="澳门" ["Taiwan"]="台湾" 
          )

          # 定义处理函数
          parse_area () {
            AREA=$1
            FILE=$2

            # 添加区域分隔符
            if [[ "$AREA" == "China" ]]; then
              echo "" >> output/playlist.m3u
              echo "# ===== 中国频道 =====" >> output/playlist.m3u
            elif [[ "$AREA" == "Hong Kong" ]]; then
              echo "" >> output/playlist.m3u
              echo "# ===== 香港频道 =====" >> output/playlist.m3u
            elif [[ "$AREA" == "Taiwan" ]]; then
              echo "" >> output/playlist.m3u
              echo "# ===== 台湾频道 =====" >> output/playlist.m3u
            fi

            # 使用 awk 提取 #EXTINF 和 URL
            awk '
              /^#EXTINF/ {
                info=$0;
                getline url;
                # 确保 URL 是 http/https 开头
                if (url ~ /^http/) {
                    print info "\t" url;
                }
              }
            ' "$FILE" | while IFS=$'\t' read -r INFO URL; do

              # 提取并清洗名称
              NAME=$(echo "$INFO" | sed 's/.*,//')
              CLEAN_NAME=$(echo "$NAME" | tr -dc '[:alnum:][:alpha:]')

              # 去重检查
              if [[ ${seen_urls[$URL]} || ${seen_names[$CLEAN_NAME]} ]]; then
                # echo "Skipping duplicate: $NAME"
                continue
              fi

              # 链接有效性检查 (仅检查 HTTP 头)
              STATUS=$(curl -I --max-time 5 -o /dev/null -s -w "%{http_code}" "$URL" || echo "000")
              if [[ "$STATUS" != "200" ]]; then
                echo "Skipping unreachable: $NAME ($URL) - Status $STATUS"
                continue
              fi

              # 标记已处理
              seen_urls[$URL]=1
              seen_names[$CLEAN_NAME]=1

              # 匹配 Logo
              LOGO_URL="https://raw.githubusercontent.com/fanmingming/live/main/tvicon/${CLEAN_NAME}.png"
              if ! curl --output /dev/null --silent --head --fail "$LOGO_URL"; then
                LOGO_URL=""
              fi

              # 匹配 EPG ID
              TVG_ID=$(grep -iF "$NAME" epg.xml | head -1 | sed -n 's/.*tvg-id="\([^"]*\)".*/\1/p')

              # 处理分组标题 (Group Title)
              if [[ "$AREA" == "China" ]]; then
                PROVINCE_KEY=$(echo "$NAME" | awk '{print $1}')
                PROVINCE_CH=${PROVINCE_MAP[$PROVINCE_KEY]}
              else
                PROVINCE_CH=""
              fi

              # 构建新的 #EXTINF 行
              EXT="#EXTINF:-1"
              [[ -n "$TVG_ID" ]] && EXT="${EXT} tvg-id=\"${TVG_ID}\""
              [[ -n "$LOGO_URL" ]] && EXT="${EXT} tvg-logo=\"${LOGO_URL}\""

              if [[ -n "$PROVINCE_CH" ]]; then
                EXT="${EXT} group-title=\"中国: ${PROVINCE_CH}\",${NAME}"
              elif [[ "$AREA" == "Hong Kong" ]]; then
                EXT="${EXT} group-title=\"香港\",${NAME}"
              elif [[ "$AREA" == "Taiwan" ]]; then
                EXT="${EXT} group-title=\"台湾\",${NAME}"
              else
                EXT="${EXT} group-title=\"${AREA}\",${NAME}"
              fi

              # 写入文件
              echo "$EXT" >> output/playlist.m3u
              echo "$URL" >> output/playlist.m3u

            done
          }

          # 循环处理所有区域
          declare -A AREAS=( ["China"]="china.m3u" ["Hong Kong"]="hongkong.m3u" ["Taiwan"]="taiwan.m3u" )
          for AREA in "${!AREAS[@]}"; do
            parse_area "$AREA" "${AREAS[$AREA]}"
          done
          
          echo "Playlist generation complete."

      # 6. Commit & Push
      - name: ⬆️ Commit and Push Playlist
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          
          # 检查文件是否有变化
          if git diff --quiet output/playlist.m3u; then
            echo "No changes detected. Skipping commit."
          else
            git add output/playlist.m3u
            git commit -m "🤖 Auto update playlist: $(date +%Y-%m-%d %H:%M:%S)" 
            git push
          fi
